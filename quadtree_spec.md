# 公共座標系タイルデータフォーマット仕様（最小データセット版）

『detailed_design.md』に準拠し、かつファイルサイズを極限まで小さく抑えるための適応型四分木（Adaptive Quadtree）タイルのデータフォーマット仕様です。
本フォーマットは、シームレスなポリゴン描画に必要不可欠な「最小限のデータセット」のみを保持します。

## 1. タイルファイル（Z/X/Y.json）の構造

通信量を最小化するため、標準的なGeoJSONなどの冗長なキー名（`type`, `properties`, `geometry`等）を排除し、独自の軽量なJSON配列フォーマットを採用します。

### JSONスキーマ
```json
[
  {
    "id": "XML内での筆ID（または一意なハッシュ）",
    "lbl": "表示用の地番文字列（例: '123番4'）",
    "pts": [
      [
        [経度1, 緯度1],
        [経度2, 緯度2],
        [経度3, 緯度3],
        ...
      ]
    ]
  },
  ...
]
```

### 各フィールドの解説
* **`id` (String)**: 元のXMLデータや詳細データと紐付けるためのキー。
* **`lbl` (String)**: ポリゴンの上に描画するための地番テキスト。※属性情報（大字・小字・精度など）はすべて削ぎ落とされています。
* **`pts` (Array of Array of Array)**: 緯度・経度（WGS84）に事前変換された、ポリゴンを構成する頂点の配列。GeoJSONのPolygonフォーマットに準拠し、最初の配列が外枠（Exterior ring）、2つ目以降の配列が中抜き（Interior rings/穴）を表します。共有境界（トポロジー）は平坦化されています。

---

## 2. 空間インデックス（カタログ）の構造
フロントエンドが「どのズームレベルで分割がストップし、どのURL（z/x/y.json）をフェッチすればよいか」を知るための軽量なインデックス構造です。

### `public_quadtree_index.json`
```json
{
  "maxZoom": 16,
  "minZoom": 12,
  "tiles": [
    "12/3551/1412",
    "13/7102/2824",
    "14/14205/5648",
    ...
  ]
}
```
* **`tiles` (Array)**: 実際にファイルとして出力され、データが存在するタイル座標（`Z/X/Y`）の完全なリスト。フロントエンドは現在表示している領域のタイル座標を計算し、このリスト内に存在するものだけを fetch します。

---

## 3. ファイルサイズの最適化による効果
* 冗長なメタデータやトポロジー構造を排除したため、1ファイルあたり3,000〜5,000筆を詰め込んでも、**Gzip圧縮時の通信量は 200KB〜400KB 程度**に収まると予測されます。
* GeoJSONの `FeatureCollection` を用いた場合と比較して、約40%〜50%のサイズ削減になります。
* 「地区外」などの不要な筆も前処理段階で除外されているため、純粋に必要な筆のみが格納されます。
