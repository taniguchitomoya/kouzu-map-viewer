# 公図シームレス表示アプリ 詳細設計書

本ドキュメントでは、国土地理院タイルをベースとした全国シームレス公図表示システムの実装に向けた、データの下処理手法およびフロントエンドのメモリ管理・効率化についての詳細設計をまとめる。

## 1. システム全体の方針
- **地図描画**: 外部ライブラリ（LeafletやMapbox GL JS等）は使用せず、既存の `../kouzu` プロジェクトのCanvas描画ロジックを拡張して実装する。
- **表示の切り替え**: 広域表示時（ズームアウト状態）では公図ポリゴンは原則描画しないが、データ密度が低いエリアについては広域表示の段階からシームレスに表示できるようにする。

---

## 2. データの下処理（前処理）設計
全国規模の公図データ（数千万〜数億ポリゴン）を扱うため、非任意座標系（公共座標系等）のデータについては事前に空間インデックスを構築する。「地域による密度の偏り」を解決するアプローチをとる。

### 2.1 データ密度に応じた適応型四分木（Adaptive Quadtree）の採用
都市部と山間部など、地域によるデータ密度の偏りを吸収するため、一律のズームレベルで分割するのではなく、**データ数（ポリゴン数）を基準として動的に分割深度を変える適応型四分木**を採用し、そのまま各区画をJSONファイルとして出力する。

- **分割のルール**:
  1. 大きな領域（低いズームレベル）からスタートし、領域内の筆（ポリゴン）数をカウントする。
  2. ポリゴン数が「閾値（例: 3,000〜5,000筆程度、Canvas描画でフリーズしない上限）」を超えている場合は、領域を4等分（ズームレベルを+1）して再帰的に処理する。
  3. ポリゴン数が閾値以下に収まった時点で分割をストップし、その領域を1つのJSONファイル（例: `data/tiles/12/3551/1412.json`）として出力する。
- **利点**:
  - 山間部などの**低密度エリアは低いズームレベル（広域）のまま1ファイルにまとまる**ため、広域表示時からフリーズすることなく一気に表示が可能になる。
  - 都市部などの高密度エリアのみが深いズームレベル（z=16〜18等）まで細分化され、パフォーマンスを担保できる。
  - 筆数に比例して自然にファイル分割が最適化されるため、無駄なファイルが生成されない。

### 2.2 空間インデックス（カタログ）の作成
クライアントが「現在の画面座標にはどのズームレベルのJSONファイルが存在するか」を知るための軽量なインデックス（目次）ツリー構造を作成する。
- 各領域の分割状況（どのズームレベルで分割がストップし、実ファイルが存在するか）を記録したJSONを最初にクライアントにロードさせることで、効率的にHTTPリクエストを行えるようにする。

### 2.3 任意座標系データの扱い
任意座標系データは絶対的な緯度経度を持たないため、シームレス地図上に直接描画することはできない。
- **メタデータの抽出**: 下処理段階で「所在（例: 東京都新宿区高田馬場○丁目）」等の情報を抽出し、「緯度経度ベースの代表点（重心など）」と紐付ける。
- **UI連携**: 地図の中心座標から逆ジオコーディング的に現在の「所在」を特定し、その所在に紐づく任意座標系データを下部ステータスバー（フッター）にリストアップする。クリック時に独立したCanvas（モーダル等）でローカル図面を描画する。

---

## 3. フロントエンドのメモリ効率化と管理
スクロールやズームによって次々と新しいJSONファイルを展開するとブラウザのヒープメモリが枯渇するため、キャッシュ管理を行う。

### 3.1 LRU (Least Recently Used) キャッシュによるメモリ解放
JavaScriptには明示的な `free()` はないが、**オブジェクトへの参照を破棄することでガベージコレクション（GC）の対象**となる。
- `Map` オブジェクトを用いて、取得・パース済みの区画データを保持する。
- キャッシュの最大保持数（例: 最大50区画分）を設定し、上限を超えた場合は最後にアクセスされてから最も時間が経過している古い区画のエントリを `Map` から `delete()` する。これにより自動的にメモリが回収される。

### 3.2 リクエストと描画の最適化
- **バッファ領域の保持**: 画面外の周囲1〜2区画分まではメモリに保持し、少し戻った際の再リクエストを防ぐ。
- **遅延ロードと間引き**: パン操作中の連続リクエストを防ぐため、移動が一時停止したタイミングでデバウンス（Debounce）処理を入れて必要な区画をフェッチする。
- **画面外カリング**: 取得した区画データであっても、現在見えているCanvas領域外のポリゴンは描画ループ内でスキップし、CPU負荷を下げる。

---

## 4. システムの実装構成（完了済）
1. **下処理スクリプト (Python)**
   - 公図XML群を読み込み、適応型四分木アルゴリズムで閾値以下になるまで分割し、ディレクトリ構造（`z/x/y.json`）としてファイル出力する。
   - 分割状況を記録した空間インデックスJSONを出力する。
   - 任意座標系のものを抽出し、所在ベースのカタログJSONを作成する。
2. **フロントエンド実装 (JavaScript / Canvas)**
   - 国土地理院タイルを背景に敷き、ズームやパン操作から現在の表示領域（Bounding Box）を取得。
   - 空間インデックスJSONを参照し、現在の表示領域に該当するJSONファイルを特定して非同期フェッチ。
   - 取得したポリゴンをLRUキャッシュを介してCanvasに描画。
3. **任意座標系UI**
   - カタログJSONから該当する任意座標系の図面リストを下部バーに表示＆ポップアップ描画。
